import { describe, it, expect } from "vitest";
import recastTailwindPlugin from "./";

describe("recastTailwindPlugin", () => {
  it("should be defined", () => {
    expect(recastTailwindPlugin).toBeDefined();
  });
});
