"use client";

import { AlertTitlePrimitive } from "@rpxl/recast-primitives";

export const AlertTitle = AlertTitlePrimitive.recast({
  base: {
    root: "mb-1 font-medium leading-none tracking-tight",
  },
});
