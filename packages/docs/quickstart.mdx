---
title: "Quickstart"
description: "Build your first styled component in 5 minutes"
---

import Button from './components/Button'

# Quickstart

Get up and running with Recast in just a few minutes. We'll build a complete button component from scratch.

## What we'll build

A flexible button component with multiple variants and states:

<Frame>
<div className="flex gap-3 items-center p-5">
  <Button variant="default" size="lg">Default Button</Button>
  <Button variant="secondary" disabled>Secondary Disabled</Button>
  <Button variant="outline">Outline Button</Button>
</div>
</Frame>

<Steps>
<Step title="Create your primitive component">
Start with a basic, unstyled button component:

```tsx ButtonPrimitive.tsx
import { forwardRef, ButtonHTMLAttributes } from 'react'

interface ButtonPrimitiveProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  children: React.ReactNode
}

export const ButtonPrimitive = forwardRef<HTMLButtonElement, ButtonPrimitiveProps>(
  ({ children, ...props }, ref) => {
    return (
      <button ref={ref} {...props}>
        {children}
      </button>
    )
  }
)
```
</Step>

<Step title="Define your styles">
Create a style definition using `recast.styles()`:

```tsx Button.styles.ts
import { recast } from '@rpxl/recast'

export const buttonStyles = recast.styles({
  base: "inline-flex items-center justify-center rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50",
  
  variants: {
    variant: {
      default: "bg-primary text-primary-foreground hover:bg-primary/90",
      destructive: "bg-destructive text-destructive-foreground hover:bg-destructive/90",
      outline: "border border-input bg-background hover:bg-accent hover:text-accent-foreground",
      secondary: "bg-secondary text-secondary-foreground hover:bg-secondary/80",
      ghost: "hover:bg-accent hover:text-accent-foreground",
      link: "text-primary underline-offset-4 hover:underline"
    },
    size: {
      default: "h-10 px-4 py-2",
      sm: "h-9 rounded-md px-3",
      lg: "h-11 rounded-md px-8",
      icon: "h-10 w-10"
    }
  },
  
  modifiers: {
    disabled: "opacity-50 cursor-not-allowed",
    loading: "cursor-wait",
    fullWidth: "w-full"
  },
  
  defaults: {
    variants: { variant: "default", size: "default" }
  }
})
```
</Step>

<Step title="Create your styled component">
Apply the styles to your primitive:

```tsx Button.tsx
import { buttonStyles } from './Button.styles'
import { ButtonPrimitive } from './ButtonPrimitive'

export const Button = buttonStyles(ButtonPrimitive)
export type ButtonProps = ComponentProps<typeof Button>
```
</Step>

<Step title="Use your component">
Now you can use your button with full TypeScript support:

<CodeGroup>
```tsx Basic Usage
<Button>Default Button</Button>
<Button variant="secondary">Secondary</Button>
<Button size="lg">Large Button</Button>
```

```tsx With Modifiers
<Button disabled>Disabled</Button>
<Button loading>Loading...</Button>
<Button fullWidth>Full Width</Button>
```

```tsx Combined
<Button variant="secondary" size="lg" fullWidth>
  Large Secondary Full-Width Button
</Button>
```
</CodeGroup>
</Step>
</Steps>

## What you get

<CardGroup cols={2}>
<Card title="Type Safety" icon="shield-check">
  Full TypeScript autocomplete and error checking for all variants and modifiers
</Card>
<Card title="Performance" icon="bolt">
  Automatic memoization means your styles are computed once and cached
</Card>
<Card title="Flexibility" icon="adjustments-horizontal">
  Easy to extend with new variants, modifiers, and conditional styling
</Card>
<Card title="Developer Experience" icon="sparkles">
  Clean API that's intuitive to use and easy to maintain
</Card>
</CardGroup>

## Advanced: Adding conditional styles

For more complex styling logic, use conditionals:

```tsx Advanced Button
const advancedButtonStyles = recast.styles({
  base: "...",
  variants: { /* ... */ },
  modifiers: { /* ... */ },
  
  conditionals: [
    {
      variants: { variant: "default" },
      modifiers: ["loading"],
      className: "bg-primary/70" // Lighter primary when default + loading
    }
  ]
})
```

## Advanced: Style extraction

Extract computed styles for use with other libraries:

<CodeGroup>
```tsx Style Extraction
// Get computed class names
const defaultButtonClasses = buttonStyles.extract({ 
  variant: "default", 
  size: "lg" 
})
// Result: "inline-flex items-center ... bg-primary text-primary-foreground ... h-11 px-8"
```

```tsx With External Libraries
// Use with Framer Motion
<motion.button 
  className={buttonStyles.extract({ variant: "default" })}
  whileHover={{ scale: 1.05 }}
>
  Animated Button
</motion.button>
```
</CodeGroup>

<Check>
**You're ready!** You now have a fully functional, type-safe, performant button component. Apply these same patterns to build any component in your design system.
</Check>

## Next Steps

<CardGroup cols={2}>
<Card title="Learn Core Concepts" icon="book-open" href="/concepts/styles">
  Dive deeper into styles, variants, and modifiers
</Card>
<Card title="Explore Composition" icon="puzzle-piece" href="/concepts/composition">
  Learn how to combine multiple style objects for complex components
</Card>
</CardGroup>